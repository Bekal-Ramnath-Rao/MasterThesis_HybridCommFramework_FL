# Optional: sudo password for host network conditions (tc).
# Copy to .env and set your password:  cp .env.example .env  then edit .env
# .env is gitignored - do not commit it.
FL_SUDO_PASSWORD=V8cNcHVO6UvS

# Optional: TLS 1.3 overhead for QUIC/HTTP3 T_calc (diagnostic pipeline).
# O_TLS_HANDSHAKE_S = one-time handshake CPU time (seconds); default 0.003 (3 ms).
# K_TLS_SEC_PER_BYTE = per-byte encrypt+decrypt CPU time (s/byte); default 2e-8 (20 ns/byte).
# O_TLS_HANDSHAKE_S=0.003
# K_TLS_SEC_PER_BYTE=2e-8

# Overlay: use external macvlan network for unified FL (replaces bridge).
# Use with bridge unified stack: docker compose -f docker-compose-unified-emotion.bridge.yml -f fl-macvlan-overlay-unified.yml ...
networks:
  fl-network:
    external: true
    name: fl-macvlan

# Docker Compose - Mental State Recognition FL with network_mode: host (high-performance, no bridge latency)
# Use this to avoid Docker bridge and reduce DDS/HTTP3 latency deviations.
# All services share the host network: use unique ports per process to avoid conflicts.
# Port assignment:
#   MQTT: broker 1883, 9001
#   AMQP: broker 5672, 15672
#   gRPC: server 50051
#   QUIC: server 4433
#   HTTP/3: server 4434
#   DDS Discovery: 7400 (participant 0), server 7650 (1), client1 7900 (2), client2 8150 (3)
# Connect to servers/brokers via 127.0.0.1 (localhost) when using host network.

services:
  # ============================================================================
  # MQTT Broker (host network: binds 1883, 9001 on host)
  # ============================================================================
  mqtt-broker-mental:
    image: eclipse-mosquitto:latest
    network_mode: host
    volumes:
      - ../mqtt-config/mosquitto.conf:/mosquitto/config/mosquitto.conf
    container_name: mqtt-broker-mental
    # Ensure Mosquitto listens on 0.0.0.0 in mosquitto.conf when using host mode

  # ============================================================================
  # MQTT Protocol Services
  # ============================================================================
  fl-server-mqtt-mental:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=MQTT
      - USE_CASE=mentalstate
      - MQTT_BROKER=127.0.0.1
      - MQTT_PORT=1883
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    volumes:
      - ../Server/MentalState_Recognition/packet_logger.db:/app/Server/MentalState_Recognition/packet_logger.db
    depends_on:
      - mqtt-broker-mental
    container_name: fl-server-mqtt-mental
    command: python -u Server/MentalState_Recognition/FL_Server_MQTT.py

  fl-client-mqtt-mental-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=MQTT
      - USE_CASE=mentalstate
      - MQTT_BROKER=127.0.0.1
      - MQTT_PORT=1883
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - mqtt-broker-mental
      - fl-server-mqtt-mental
    container_name: fl-client-mqtt-mental-1
    command: python -u Client/MentalState_Recognition/FL_Client_MQTT.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']
    volumes:
      - ../Client/MentalState_Recognition/packet_logger.db:/app/Client/MentalState_Recognition/packet_logger.db

  fl-client-mqtt-mental-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=MQTT
      - USE_CASE=mentalstate
      - MQTT_BROKER=127.0.0.1
      - MQTT_PORT=1883
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - mqtt-broker-mental
      - fl-server-mqtt-mental
    container_name: fl-client-mqtt-mental-2
    command: python -u Client/MentalState_Recognition/FL_Client_MQTT.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']
    volumes:
      - ../Client/MentalState_Recognition/packet_logger.db:/app/Client/MentalState_Recognition/packet_logger.db

  # ============================================================================
  # AMQP Broker (host network: binds 5672, 15672 on host)
  # ============================================================================
  rabbitmq-mental:
    image: rabbitmq:3.12-management
    network_mode: host
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    container_name: rabbitmq-mental

  # ============================================================================
  # AMQP Protocol Services
  # ============================================================================
  fl-server-amqp-mental:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=AMQP
      - USE_CASE=mentalstate
      - AMQP_HOST=127.0.0.1
      - AMQP_PORT=5672
      - AMQP_USER=guest
      - AMQP_PASSWORD=guest
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - rabbitmq-mental
    container_name: fl-server-amqp-mental
    command: python -u Server/MentalState_Recognition/FL_Server_AMQP.py

  fl-client-amqp-mental-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=AMQP
      - USE_CASE=mentalstate
      - AMQP_HOST=127.0.0.1
      - AMQP_PORT=5672
      - AMQP_USER=guest
      - AMQP_PASSWORD=guest
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - rabbitmq-mental
      - fl-server-amqp-mental
    container_name: fl-client-amqp-mental-1
    command: python -u Client/MentalState_Recognition/FL_Client_AMQP.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-amqp-mental-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=AMQP
      - USE_CASE=mentalstate
      - AMQP_HOST=127.0.0.1
      - AMQP_PORT=5672
      - AMQP_USER=guest
      - AMQP_PASSWORD=guest
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - rabbitmq-mental
      - fl-server-amqp-mental
    container_name: fl-client-amqp-mental-2
    command: python -u Client/MentalState_Recognition/FL_Client_AMQP.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  # ============================================================================
  # gRPC Protocol Services (server binds 50051 on host)
  # ============================================================================
  fl-server-grpc-mental:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=gRPC
      - USE_CASE=mentalstate
      - GRPC_HOST=0.0.0.0
      - GRPC_PORT=50051
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    container_name: fl-server-grpc-mental
    command: python -u Server/MentalState_Recognition/FL_Server_gRPC.py

  fl-client-grpc-mental-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=gRPC
      - USE_CASE=mentalstate
      - GRPC_HOST=127.0.0.1
      - GRPC_PORT=50051
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-grpc-mental
    container_name: fl-client-grpc-mental-1
    command: python -u Client/MentalState_Recognition/FL_Client_gRPC.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-grpc-mental-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=gRPC
      - USE_CASE=mentalstate
      - GRPC_HOST=127.0.0.1
      - GRPC_PORT=50051
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-grpc-mental
    container_name: fl-client-grpc-mental-2
    command: python -u Client/MentalState_Recognition/FL_Client_gRPC.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  # ============================================================================
  # QUIC Protocol Services (server binds 4433/UDP on host)
  # ============================================================================
  fl-server-quic-mental:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=QUIC
      - USE_CASE=mentalstate
      - QUIC_HOST=0.0.0.0
      - QUIC_PORT=4433
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
      - QUIC_RECV_BUFFER=${QUIC_RECV_BUFFER:-2097152}
      - QUIC_SEND_BUFFER=${QUIC_SEND_BUFFER:-2097152}
    cap_add:
      - NET_ADMIN
    volumes:
      - ../certs:/app/certs:ro
    container_name: fl-server-quic-mental
    command: python -u Server/MentalState_Recognition/FL_Server_QUIC.py

  fl-client-quic-mental-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=QUIC
      - USE_CASE=mentalstate
      - QUIC_HOST=127.0.0.1
      - QUIC_PORT=4433
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-quic-mental
    container_name: fl-client-quic-mental-1
    command: python -u Client/MentalState_Recognition/FL_Client_QUIC.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-quic-mental-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=QUIC
      - USE_CASE=mentalstate
      - QUIC_HOST=127.0.0.1
      - QUIC_PORT=4433
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-quic-mental
    container_name: fl-client-quic-mental-2
    command: python -u Client/MentalState_Recognition/FL_Client_QUIC.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  # ============================================================================
  # HTTP/3 Protocol Services (server binds 4434 on host)
  # ============================================================================
  fl-server-http3-mental:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=HTTP3
      - USE_CASE=mentalstate
      - HTTP3_HOST=0.0.0.0
      - HTTP3_PORT=4434
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
      - QUIC_RECV_BUFFER=${QUIC_RECV_BUFFER:-2097152}
      - QUIC_SEND_BUFFER=${QUIC_SEND_BUFFER:-2097152}
    cap_add:
      - NET_ADMIN
    volumes:
      - ../certs:/app/certs:ro
      - ../Server/MentalState_Recognition/results:/app/Server/MentalState_Recognition/results
    container_name: fl-server-http3-mental
    command: python -u Server/MentalState_Recognition/FL_Server_HTTP3.py

  fl-client-http3-mental-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=HTTP3
      - USE_CASE=mentalstate
      - HTTP3_HOST=127.0.0.1
      - HTTP3_PORT=4434
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-http3-mental
    container_name: fl-client-http3-mental-1
    command: python -u Client/MentalState_Recognition/FL_Client_HTTP3.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-http3-mental-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=HTTP3
      - USE_CASE=mentalstate
      - HTTP3_HOST=127.0.0.1
      - HTTP3_PORT=4434
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-http3-mental
    container_name: fl-client-http3-mental-2
    command: python -u Client/MentalState_Recognition/FL_Client_HTTP3.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  # ============================================================================
  # DDS Discovery Server (unicast peer on 127.0.0.1:7400)
  # ============================================================================
  dds-discovery-mental:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - DDS_DOMAIN_ID=0
      - CYCLONEDDS_URI=file:///app/config/cyclonedds-discovery-server.xml
    volumes:
      - ../config:/app/config:ro
    container_name: dds-discovery-mental
    command: python -u scripts/dds_discovery_server.py
    # Start before DDS server and clients

  # ============================================================================
  # DDS Protocol Services (Discovery Server at 127.0.0.1:7400; server 7650, client1 7900, client2 8150)
  # ============================================================================
  fl-server-dds-mental:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=DDS
      - USE_CASE=mentalstate
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - LOCAL_EPOCHS=20
      - DDS_DOMAIN_ID=0
      - CYCLONEDDS_URI=file:///app/config/cyclonedds-emotion-server.xml
      - ROS_DISCOVERY_SERVER=127.0.0.1:7400
      - DDS_USE_PRIMITIVE_CONFIG=1
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    volumes:
      - ../config:/app/config:ro
      - ../Server/MentalState_Recognition/results:/app/Server/MentalState_Recognition/results
    depends_on:
      - dds-discovery-mental
    container_name: fl-server-dds-mental
    command: bash -c "sleep 5 && python -u Server/MentalState_Recognition/FL_Server_DDS.py"

  fl-client-dds-mental-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=DDS
      - USE_CASE=mentalstate
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - DDS_DOMAIN_ID=0
      - CYCLONEDDS_URI=file:///app/config/cyclonedds-emotion-client1.xml
      - ROS_DISCOVERY_SERVER=127.0.0.1:7400
      - DDS_USE_PRIMITIVE_CONFIG=1
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    depends_on:
      - dds-discovery-mental
      - fl-server-dds-mental
    cap_add:
      - NET_ADMIN
    volumes:
      - ../config:/app/config:ro
      - ../Client/MentalState_Recognition/Dataset:/app/Client/MentalState_Recognition/Dataset
    container_name: fl-client-dds-mental-1
    command: python -u Client/MentalState_Recognition/FL_Client_DDS.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-dds-mental-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=DDS
      - USE_CASE=mentalstate
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - DDS_DOMAIN_ID=0
      - CYCLONEDDS_URI=file:///app/config/cyclonedds-emotion-client2.xml
      - ROS_DISCOVERY_SERVER=127.0.0.1:7400
      - DDS_USE_PRIMITIVE_CONFIG=1
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    depends_on:
      - dds-discovery-mental
      - fl-server-dds-mental
    cap_add:
      - NET_ADMIN
    volumes:
      - ../config:/app/config:ro
      - ../Client/MentalState_Recognition/Dataset:/app/Client/MentalState_Recognition/Dataset
    container_name: fl-client-dds-mental-2
    command: python -u Client/MentalState_Recognition/FL_Client_DDS.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

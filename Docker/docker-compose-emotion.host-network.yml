# Docker Compose - Emotion FL with network_mode: host (high-performance, no bridge latency)
# Use this to avoid Docker bridge and reduce DDS/HTTP3 latency deviations.
# All services share the host network: use unique ports per process to avoid conflicts.
# Port assignment:
#   MQTT: broker 1883, 9001
#   AMQP: broker 5672, 15672
#   gRPC: server 50051
#   QUIC: server 4433
#   HTTP/3: server 4434
#   DDS Discovery: 7410 (participant 0), server 7412 (1), client1 7414 (2), client2 7416 (3)
# Connect to servers/brokers via 127.0.0.1 (localhost) when using host network.

services:
  # ============================================================================
  # MQTT Broker (host network: binds 1883, 9001 on host)
  # ============================================================================
  mqtt-broker:
    image: eclipse-mosquitto:latest
    network_mode: host
    volumes:
      - ../mqtt-config/mosquitto.conf:/mosquitto/config/mosquitto.conf
    container_name: mqtt-broker
    # Ensure Mosquitto listens on 0.0.0.0 in mosquitto.conf when using host mode

  # ============================================================================
  # MQTT Protocol Services
  # ============================================================================
  fl-server-mqtt-emotion:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=MQTT
      - USE_CASE=emotion
      - MQTT_BROKER=127.0.0.1
      - MQTT_PORT=1883
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - NETWORK_SCENARIO=${NETWORK_SCENARIO:-default}
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    volumes:
      - ../Server/Emotion_Recognition/packet_logger.db:/app/Server/Emotion_Recognition/packet_logger.db
    depends_on:
      - mqtt-broker
    container_name: fl-server-mqtt-emotion
    command: python -u Server/Emotion_Recognition/FL_Server_MQTT.py

  fl-client-mqtt-emotion-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=MQTT
      - USE_CASE=emotion
      - MQTT_BROKER=127.0.0.1
      - MQTT_PORT=1883
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - mqtt-broker
      - fl-server-mqtt-emotion
    container_name: fl-client-mqtt-emotion-1
    command: python -u Client/Emotion_Recognition/FL_Client_MQTT.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']
    volumes:
      - ../Client/Emotion_Recognition/packet_logger.db:/app/Client/Emotion_Recognition/packet_logger.db

  fl-client-mqtt-emotion-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=MQTT
      - USE_CASE=emotion
      - MQTT_BROKER=127.0.0.1
      - MQTT_PORT=1883
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - mqtt-broker
      - fl-server-mqtt-emotion
    container_name: fl-client-mqtt-emotion-2
    command: python -u Client/Emotion_Recognition/FL_Client_MQTT.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']
    volumes:
      - ../Client/Emotion_Recognition/packet_logger.db:/app/Client/Emotion_Recognition/packet_logger.db

  # ============================================================================
  # AMQP Broker (host network: binds 5672, 15672 on host)
  # ============================================================================
  amqp-broker:
    image: rabbitmq:3.12-management
    network_mode: host
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    container_name: amqp-broker

  # ============================================================================
  # AMQP Protocol Services
  # ============================================================================
  fl-server-amqp-emotion:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=AMQP
      - USE_CASE=emotion
      - AMQP_HOST=127.0.0.1
      - AMQP_PORT=5672
      - AMQP_USER=guest
      - AMQP_PASSWORD=guest
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - NETWORK_SCENARIO=${NETWORK_SCENARIO:-default}
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - amqp-broker
    container_name: fl-server-amqp-emotion
    command: python -u Server/Emotion_Recognition/FL_Server_AMQP.py

  fl-client-amqp-emotion-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=AMQP
      - USE_CASE=emotion
      - AMQP_HOST=127.0.0.1
      - AMQP_PORT=5672
      - AMQP_USER=guest
      - AMQP_PASSWORD=guest
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - amqp-broker
      - fl-server-amqp-emotion
    container_name: fl-client-amqp-emotion-1
    command: python -u Client/Emotion_Recognition/FL_Client_AMQP.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-amqp-emotion-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=AMQP
      - USE_CASE=emotion
      - AMQP_HOST=127.0.0.1
      - AMQP_PORT=5672
      - AMQP_USER=guest
      - AMQP_PASSWORD=guest
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - amqp-broker
      - fl-server-amqp-emotion
    container_name: fl-client-amqp-emotion-2
    command: python -u Client/Emotion_Recognition/FL_Client_AMQP.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  # ============================================================================
  # gRPC Protocol Services (server binds 50051 on host)
  # ============================================================================
  fl-server-grpc-emotion:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=gRPC
      - USE_CASE=emotion
      - GRPC_HOST=0.0.0.0
      - GRPC_PORT=50051
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - NETWORK_SCENARIO=${NETWORK_SCENARIO:-default}
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    container_name: fl-server-grpc-emotion
    command: python -u Server/Emotion_Recognition/FL_Server_gRPC.py

  fl-client-grpc-emotion-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=gRPC
      - USE_CASE=emotion
      - GRPC_HOST=127.0.0.1
      - GRPC_PORT=50051
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-grpc-emotion
    container_name: fl-client-grpc-emotion-1
    command: python -u Client/Emotion_Recognition/FL_Client_gRPC.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-grpc-emotion-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=gRPC
      - USE_CASE=emotion
      - GRPC_HOST=127.0.0.1
      - GRPC_PORT=50051
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-grpc-emotion
    container_name: fl-client-grpc-emotion-2
    command: python -u Client/Emotion_Recognition/FL_Client_gRPC.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  # ============================================================================
  # QUIC Protocol Services (server binds 4433/UDP on host)
  # ============================================================================
  fl-server-quic-emotion:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=QUIC
      - USE_CASE=emotion
      - QUIC_HOST=0.0.0.0
      - QUIC_PORT=4433
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - NETWORK_SCENARIO=${NETWORK_SCENARIO:-default}
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
      - QUIC_RECV_BUFFER=${QUIC_RECV_BUFFER:-2097152}
      - QUIC_SEND_BUFFER=${QUIC_SEND_BUFFER:-2097152}
    cap_add:
      - NET_ADMIN
    volumes:
      - ../certs:/app/certs:ro
    container_name: fl-server-quic-emotion
    command: python -u Server/Emotion_Recognition/FL_Server_QUIC.py

  fl-client-quic-emotion-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=QUIC
      - USE_CASE=emotion
      - QUIC_HOST=127.0.0.1
      - QUIC_PORT=4433
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-quic-emotion
    container_name: fl-client-quic-emotion-1
    command: python -u Client/Emotion_Recognition/FL_Client_QUIC.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-quic-emotion-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=QUIC
      - USE_CASE=emotion
      - QUIC_HOST=127.0.0.1
      - QUIC_PORT=4433
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-quic-emotion
    container_name: fl-client-quic-emotion-2
    command: python -u Client/Emotion_Recognition/FL_Client_QUIC.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  # ============================================================================
  # HTTP/3 Protocol Services (server binds 4434 on host)
  # ============================================================================
  fl-server-http3-emotion:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=HTTP3
      - USE_CASE=emotion
      - HTTP3_HOST=0.0.0.0
      - HTTP3_PORT=4434
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - NETWORK_SCENARIO=${NETWORK_SCENARIO:-default}
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
      - QUIC_RECV_BUFFER=${QUIC_RECV_BUFFER:-2097152}
      - QUIC_SEND_BUFFER=${QUIC_SEND_BUFFER:-2097152}
    cap_add:
      - NET_ADMIN
    volumes:
      - ../certs:/app/certs:ro
      - ../Server/Emotion_Recognition/results:/app/Server/Emotion_Recognition/results
    container_name: fl-server-http3-emotion
    command: python -u Server/Emotion_Recognition/FL_Server_HTTP3.py

  fl-client-http3-emotion-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=HTTP3
      - USE_CASE=emotion
      - HTTP3_HOST=127.0.0.1
      - HTTP3_PORT=4434
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-http3-emotion
    container_name: fl-client-http3-emotion-1
    command: python -u Client/Emotion_Recognition/FL_Client_HTTP3.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-http3-emotion-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=2
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=HTTP3
      - USE_CASE=emotion
      - HTTP3_HOST=127.0.0.1
      - HTTP3_PORT=4434
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    depends_on:
      - fl-server-http3-emotion
    container_name: fl-client-http3-emotion-2
    command: python -u Client/Emotion_Recognition/FL_Client_HTTP3.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  # ============================================================================
  # DDS Discovery Server (unicast peer on 127.0.0.1:7410)
  # Only used in single-machine (local) mode. For distributed 3-PC mode, do NOT start this
  # service; instead set DDS_PEER_* env vars (see below) and let dds_distributed_unicast.py
  # generate the Cyclone XML automatically.
  # ============================================================================
  dds-discovery-emotion:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - DDS_DOMAIN_ID=0
      - CYCLONEDDS_URI=file:///app/config/cyclonedds-discovery-server.xml
    volumes:
      - ../config:/app/config:ro
    container_name: dds-discovery-emotion
    command: python -u scripts/dds_discovery_server.py
    # Start before DDS server and clients

  # ============================================================================
  # DDS Protocol Services
  #
  # LOCAL mode (single machine): uses dds-discovery-emotion on 127.0.0.1:7410.
  #   CYCLONEDDS_URI is set to the static local XML; DDS_PEER_* are not needed.
  #
  # DISTRIBUTED 3-PC mode (192.168.0.x): do NOT start dds-discovery-emotion.
  #   Set the following before running docker compose:
  #     export DDS_PEER_SERVER=192.168.0.102
  #     export DDS_PEER_CLIENT1=192.168.0.102   # co-located with server
  #     export DDS_PEER_CLIENT2=192.168.0.100   # Windows PC
  #     export DDS_PEER_CLIENT3=192.168.0.101   # Linux PC
  #     export CYCLONEDDS_URI=""                # must be empty so dds_distributed_unicast.py activates
  #   The server and local client (client1) are started by:
  #     docker compose ... up -d fl-server-dds-emotion fl-client-dds-emotion-1
  #   Remote clients 2 and 3 are started via distributed_client_gui.py on their respective PCs.
  #   SPDP ports: server 7412, client1 7414, client2 7416, client3 7418.
  #   Open UDP 7400-7500 on all machines' firewalls.
  # ============================================================================
  fl-server-dds-emotion:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    network_mode: host
    environment:
      - PROTOCOL=DDS
      - USE_CASE=emotion
      - NUM_ROUNDS=${NUM_ROUNDS:-1000}
      - NETWORK_SCENARIO=${NETWORK_SCENARIO:-default}
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - MIN_CLIENTS=${MIN_CLIENTS:-2}
      - MAX_CLIENTS=${MAX_CLIENTS:-100}
      - CUDA_VISIBLE_DEVICES=
      - LOCAL_EPOCHS=20
      - DDS_DOMAIN_ID=0
      # For local mode: static XML via discovery server.
      # For distributed mode: unset CYCLONEDDS_URI and set DDS_PEER_* — dds_distributed_unicast.py takes over.
      - CYCLONEDDS_URI=${CYCLONEDDS_URI:-file:///app/config/cyclonedds-emotion-server.xml}
      - ROS_DISCOVERY_SERVER=${ROS_DISCOVERY_SERVER:-127.0.0.1:7410}
      - DDS_USE_PRIMITIVE_CONFIG=1
      # DDS_PEER_* for distributed mode (ignored when CYCLONEDDS_URI is set above)
      - DDS_PEER_SERVER=${DDS_PEER_SERVER:-}
      - DDS_PEER_CLIENT1=${DDS_PEER_CLIENT1:-}
      - DDS_PEER_CLIENT2=${DDS_PEER_CLIENT2:-}
      - DDS_PEER_CLIENT3=${DDS_PEER_CLIENT3:-}
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    cap_add:
      - NET_ADMIN
    volumes:
      - ../config:/app/config:ro
      - ../Server/Emotion_Recognition/results:/app/Server/Emotion_Recognition/results
    depends_on:
      - dds-discovery-emotion
    container_name: fl-server-dds-emotion
    command: bash -c "sleep 15 && python -u Server/Emotion_Recognition/FL_Server_DDS.py"

  fl-client-dds-emotion-1:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=1
      - NUM_CLIENTS=${NUM_CLIENTS:-2}
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=DDS
      - USE_CASE=emotion
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - DDS_DOMAIN_ID=0
      - CYCLONEDDS_URI=${CYCLONEDDS_URI:-file:///app/config/cyclonedds-emotion-client1.xml}
      - ROS_DISCOVERY_SERVER=${ROS_DISCOVERY_SERVER:-127.0.0.1:7410}
      - DDS_USE_PRIMITIVE_CONFIG=1
      - DDS_PEER_SERVER=${DDS_PEER_SERVER:-}
      - DDS_PEER_CLIENT1=${DDS_PEER_CLIENT1:-}
      - DDS_PEER_CLIENT2=${DDS_PEER_CLIENT2:-}
      - DDS_PEER_CLIENT3=${DDS_PEER_CLIENT3:-}
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    depends_on:
      - dds-discovery-emotion
      - fl-server-dds-emotion
    cap_add:
      - NET_ADMIN
    volumes:
      - ../config:/app/config:ro
      - ../Client/Emotion_Recognition/Dataset:/app/Client/Emotion_Recognition/Dataset
    container_name: fl-client-dds-emotion-1
    command: python -u Client/Emotion_Recognition/FL_Client_DDS.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

  fl-client-dds-emotion-2:
    build:
      context: ..
      dockerfile: Client/Dockerfile
    network_mode: host
    environment:
      - CLIENT_ID=2
      - NUM_CLIENTS=${NUM_CLIENTS:-2}
      - STOP_ON_CLIENT_CONVERGENCE=${STOP_ON_CLIENT_CONVERGENCE:-true}
      - TRAINING_TERMINATION_MODE=${TRAINING_TERMINATION_MODE:-}
      - FL_DIAGNOSTIC_PIPELINE=${FL_DIAGNOSTIC_PIPELINE:-0}
      - PROTOCOL=DDS
      - USE_CASE=emotion
      - CUDA_VISIBLE_DEVICES=0
      - GPU_DEVICE_ID=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - TF_GPU_MEMORY_FRACTION=0.9
      - DDS_DOMAIN_ID=0
      - CYCLONEDDS_URI=${CYCLONEDDS_URI:-file:///app/config/cyclonedds-emotion-client2.xml}
      - ROS_DISCOVERY_SERVER=${ROS_DISCOVERY_SERVER:-127.0.0.1:7410}
      - DDS_USE_PRIMITIVE_CONFIG=1
      - DDS_PEER_SERVER=${DDS_PEER_SERVER:-}
      - DDS_PEER_CLIENT1=${DDS_PEER_CLIENT1:-}
      - DDS_PEER_CLIENT2=${DDS_PEER_CLIENT2:-}
      - DDS_PEER_CLIENT3=${DDS_PEER_CLIENT3:-}
      - USE_QUANTIZATION=${USE_QUANTIZATION:-false}
      - QUANTIZATION_STRATEGY=${QUANTIZATION_STRATEGY:-parameter_quantization}
      - QUANTIZATION_BITS=${QUANTIZATION_BITS:-8}
      - QUANTIZATION_SYMMETRIC=${QUANTIZATION_SYMMETRIC:-true}
      - QUANTIZATION_PER_CHANNEL=${QUANTIZATION_PER_CHANNEL:-false}
    depends_on:
      - dds-discovery-emotion
      - fl-server-dds-emotion
    cap_add:
      - NET_ADMIN
    volumes:
      - ../config:/app/config:ro
      - ../Client/Emotion_Recognition/Dataset:/app/Client/Emotion_Recognition/Dataset
    container_name: fl-client-dds-emotion-2
    command: python -u Client/Emotion_Recognition/FL_Client_DDS.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              driver: nvidia
              device_ids: ['0']

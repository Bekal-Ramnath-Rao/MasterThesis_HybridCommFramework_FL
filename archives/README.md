# Archives Directory

This directory contains archived files and compressed packages.

## Files

- `protoc.zip` - Protocol buffer compiler archive

## Usage

### Extracting Archives
```bash
# Extract protoc
unzip archives/protoc.zip
```

## Notes

- This directory is for storing downloaded packages and archives that are needed but not actively used
- Consider cleaning up archives after extraction if they are no longer needed
